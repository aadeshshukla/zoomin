// Top-level build file for the GestureZoomCamera project.
// Plugins are declared but not applied here; each module applies what it needs.
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
}
